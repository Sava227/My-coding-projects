let text = "Hello World!";  //make the string variable which contains something

console.log(text);      //output the string variable
console.log(text.length);     //how long the text is
console.log(text.toUpperCase()); //all uppercase letters
console.log(text[1]);   //which letter is going to be shown in the console, starting with 0 as the 1st letter
console.log(text.includes("hello"));       //finds the "hello" in the text